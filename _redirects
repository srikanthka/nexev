# ═══ NexEV — Cloudflare Pages _redirects ═══
# Docs: https://developers.cloudflare.com/pages/configuration/redirects/

# ── 301 Permanent redirects (fixes "Page with redirect" in GSC) ──────
# The legacy /pages/* corporate site has been removed. Redirect any
# already-indexed URLs to the homepage so they don't 404.
/pages/*                      /                         301
/training                     /                         301
/solutions                    /                         301

# ── Product pages are now at root level (e.g. /xt60h-connector.html) ──
# Redirect any old /products/* URLs to root-level equivalents
/products/:slug.html  /:slug.html             301
/products/:slug       /:slug.html             301
